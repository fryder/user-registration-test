apiVersion: apps/v1
kind: Deployment
metadata:
  name: develivery-management
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: develivery-management
  template:
    metadata:
      labels:
        app: develivery-management
    spec:
      containers:
        - name: develivery-management
          image: develivery-management:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: 512Mi
              cpu: '1'

---
apiVersion: v1
kind: Service
metadata:
  name: develivery-management-balancer
spec:
  selector:
    app: develivery-management
  ports:
    - protocol: TCP
      port: 8000 # The port to expose to the outside
      targetPort: 8080 # The port the application is running on in the pods
  type: LoadBalancer
  sessionAffinity: ClientIP
